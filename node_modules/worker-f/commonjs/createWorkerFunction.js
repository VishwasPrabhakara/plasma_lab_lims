"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = createWorkerFunction;
var _createWorkerFunction_ = _interopRequireDefault(require("./createWorkerFunction_.js"));
function _interopRequireDefault(e) { return e && e.__esModule ? e : { "default": e }; }
function createWorkerFunction(createWorkerInEnvironment, fnOrAlias) {
  // When the worker function returns a monolithic response,
  // use `resolveCall()`/`rejectCall()` functions to report the response to the main thread.
  // These functions will `resolve` or `reject` the `Promise` returned from `.call()` or `.callOnce()`.
  var resolveCall = undefined;
  var rejectCall = undefined;
  var createMethods = function createMethods(start, stop, started, stopped, sendToWorker, mustHaveStarted, mustNotHaveStarted, mustNotHaveStopped) {
    return {
      // Calls the function. Could be used multiple times.
      call: function call() {
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        mustNotHaveStopped();
        mustHaveStarted();
        if (resolveCall || rejectCall) {
          throw new Error('Previous call not finished');
        }
        // isStreamingFn = false
        return new Promise(function (resolve, reject) {
          resolveCall = resolve;
          rejectCall = reject;
          sendToWorker(args);
        });
      },
      // Calls the function once.
      callOnce: function callOnce() {
        start();
        return this.call.apply(this, arguments)["finally"](stop);
      }
    };
  };
  // This function will be executed in the worker thread.
  // It will be stringified and injected in the worker source code.
  // Therefore, it should be a "self-contained" function,
  // i.e. it shouldn't reference anything outside of its body.
  var createInputHandler = function createInputHandler(fn, send) {
    var isPromise = function isPromise(anything) {
      return anything !== null && typeof anything === 'object' && typeof anything.then === 'function';
    };
    return function (args) {
      var result = fn.apply(void 0, args);
      if (isPromise(result)) {
        result.then(send);
      } else {
        send(result);
      }
    };
  };
  // This function will be executed in the main thread.
  // Currently, we are in the main thread.
  var handleError = function handleError(error) {
    if (rejectCall) {
      rejectCall(error);
      resolveCall = undefined;
      rejectCall = undefined;
    } else {
      throw new Error('`reject` callback not found');
    }
  };
  // This function will be executed in the main thread.
  // Currently, we are in the main thread.
  var handleOutput = function handleOutput(output) {
    if (resolveCall) {
      resolveCall(output);
      resolveCall = undefined;
      rejectCall = undefined;
    } else {
      throw new Error('`resolve` callback not found');
    }
  };
  return (0, _createWorkerFunction_["default"])(createWorkerInEnvironment, fnOrAlias, createMethods, createInputHandler, handleError, handleOutput);
}
//# sourceMappingURL=createWorkerFunction.js.map